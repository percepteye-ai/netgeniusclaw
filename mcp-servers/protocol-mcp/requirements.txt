# Upper bound is LOAD-BEARING: mcp 2.0.0 removed mcp.server.fastmcp
# (FastMCP moved to the standalone `fastmcp` distribution), and this server
# imports `from mcp.server.fastmcp import FastMCP`.
#
# The former `fastmcp>=0.1.0` line was REMOVED by spec 077: nothing here imports
# `fastmcp` (same dead-pin pattern found in n2n-mcp). Without <2 a fresh install
# resolves 2.x and dies on import. Do not 'tidy' this bound away. Spec 077.
# Protocol MCP Server — core dependencies
scapy>=2.5.0
networkx>=3.0
mcp>=1.0.0,<2

# feature 066: NCFED edge node (NetClaw Mobile) — WebSocket transport for
# phone connections, QR rendering for enrollment tokens, and the
# disconnected-device push-notification fallback (FCM/APNs; APNs requires
# HTTP/2, hence h2)
#
# Upper bound is LOAD-BEARING (same dead-pin pattern as mcp above, spec 077):
# feature 116's gateway_ws.py imports websockets.protocol.State, a SUBMODULE
# introduced in the v13+ sans-I/O rewrite. A future major could remove or
# relocate it exactly like mcp 2.0.0 removed mcp.server.fastmcp. Without <17
# a fresh install could resolve a version where that submodule no longer
# exists in this shape.
websockets>=12.0,<17
qrcode>=7.4
httpx>=0.27
h2>=4.1
