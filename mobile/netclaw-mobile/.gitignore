# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Firebase project configuration (push notifications). These carry the
# operator's own project/sender IDs and API keys and are per-deployment, not
# per-repo — every operator drops in their own. The build works without them
# (the Google Services Gradle plugin is applied only when the JSON is present),
# it just cannot register for push. See README.md "Push notifications".
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist
firebase_options.dart

# App Store Connect screenshot staging (docs/MOBILE-RELEASE.md step 6) --
# captured locally per release, uploaded directly to App Store Connect, not
# repo content.
/screenshots/
